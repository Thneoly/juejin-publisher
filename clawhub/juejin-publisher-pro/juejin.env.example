# 使用方法：把本文件复制为 .juejin.env（已被 .gitignore 排除，绝不入库），填入你的 Cookie
#
# Cookie 获取：浏览器登录 juejin.cn → F12 开发者工具 → Network → 随便点一个 api.juejin.cn 的请求
# → Request Headers → Cookie 字段里找到 sessionid=xxx，整段复制进来（只贴 sessionid 值也行）
#
# 有效期约 30 天。过期后接口报 401，重新登录复制一次即可。
JUEJIN_COOKIE=sessionid=把这里替换成你的值
